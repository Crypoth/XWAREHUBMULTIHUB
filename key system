local Luna = loadstring(game:HttpGet("https://raw.githubusercontent.com/Nebula-Softworks/Luna-Interface-Suite/refs/heads/main/source.lua", true))()

local KeyGui = Instance.new("ScreenGui")
KeyGui.Name = "XWARE_KeySystem"
KeyGui.ResetOnSpawn = false
KeyGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
KeyGui.Parent = game:GetService("CoreGui")

local KeyFrame = Instance.new("Frame")
KeyFrame.Name = "KeyFrame"
KeyFrame.Size = UDim2.new(0, 500, 0, 450)
KeyFrame.Position = UDim2.new(0.5, -250, 0.5, -225)
KeyFrame.AnchorPoint = Vector2.new(0, 0)
KeyFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
KeyFrame.BackgroundTransparency = 0.15
KeyFrame.Parent = KeyGui
KeyFrame.Active = true
KeyFrame.Draggable = true
KeyFrame.ZIndex = 2

-- Round frame corners
local Corner = Instance.new("UICorner", KeyFrame)
Corner.CornerRadius = UDim.new(0, 14)

-- Thicker flower-like outline effect
local OutlineFrame = Instance.new("Frame")
OutlineFrame.Size = UDim2.new(1, 20, 1, 20)  -- Increased size for a thicker outline
OutlineFrame.Position = UDim2.new(0, -10, 0, -10)  -- Adjusted position to match thicker outline
OutlineFrame.BackgroundTransparency = 1
OutlineFrame.Parent = KeyFrame

-- Create flower-like effect with UIGradient
local FlowerOutline = Instance.new("UIGradient", OutlineFrame)
FlowerOutline.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(120, 100, 200)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(80, 60, 120)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(120, 80, 180))
})
FlowerOutline.Rotation = 45  -- Rotated to simulate the flower-like effect

-- Title
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 40)
Title.Position = UDim2.new(0, 0, 0, 10)
Title.BackgroundTransparency = 1
Title.Text = "XWARE HUB — KEY SYSTEM"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 28
Title.Parent = KeyFrame
Title.ZIndex = 3

-- Subtitle
local Subtitle = Instance.new("TextLabel")
Subtitle.Size = UDim2.new(1, 0, 0, 25)
Subtitle.Position = UDim2.new(0, 0, 0, 50)
Subtitle.BackgroundTransparency = 1
Subtitle.Text = "Enter your key to unlock the hub features below."
Subtitle.TextColor3 = Color3.fromRGB(180, 180, 180)
Subtitle.Font = Enum.Font.GothamBold
Subtitle.TextSize = 16
Subtitle.Parent = KeyFrame
Subtitle.ZIndex = 3

-- Key Input Box
local KeyBox = Instance.new("TextBox")
KeyBox.Size = UDim2.new(0, 350, 0, 45)
KeyBox.Position = UDim2.new(0.5, -175, 0, 110)
KeyBox.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
KeyBox.PlaceholderText = "Enter Key Here!"  -- Placeholder text
KeyBox.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyBox.TextSize = 20
KeyBox.Font = Enum.Font.GothamBold
KeyBox.ClearTextOnFocus = true
KeyBox.Parent = KeyFrame
KeyBox.ZIndex = 3

-- Remove any potential "TextBox" or other conflicting elements
Instance.new("UICorner", KeyBox).CornerRadius = UDim.new(0, 10)
Instance.new("UIStroke", KeyBox).Color = Color3.fromRGB(80, 80, 100)

-- Submit Key Button
local SubmitButton = Instance.new("TextButton")
SubmitButton.Size = UDim2.new(0, 160, 0, 45)
SubmitButton.Position = UDim2.new(0.5, -170, 0, 180)
SubmitButton.Text = "Unlock"
SubmitButton.Font = Enum.Font.GothamBold
SubmitButton.TextSize = 18
SubmitButton.TextColor3 = Color3.fromRGB(255, 255, 255)
SubmitButton.BackgroundColor3 = Color3.fromRGB(90, 70, 180)
SubmitButton.Parent = KeyFrame
SubmitButton.ZIndex = 3

Instance.new("UICorner", SubmitButton).CornerRadius = UDim.new(0, 10)
Instance.new("UIStroke", SubmitButton).Color = Color3.fromRGB(120, 100, 200)

-- Get Key Button
local GetKeyButton = Instance.new("TextButton")
GetKeyButton.Size = UDim2.new(0, 160, 0, 45)
GetKeyButton.Position = UDim2.new(0.5, 10, 0, 180)
GetKeyButton.Text = "Get Key"
GetKeyButton.Font = Enum.Font.GothamBold
GetKeyButton.TextSize = 18
GetKeyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
GetKeyButton.BackgroundColor3 = Color3.fromRGB(50, 50, 90)
GetKeyButton.Parent = KeyFrame
GetKeyButton.ZIndex = 3

Instance.new("UICorner", GetKeyButton).CornerRadius = UDim.new(0, 10)
Instance.new("UIStroke", GetKeyButton).Color = Color3.fromRGB(90, 90, 120)

-- Status Label
local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(1, -20, 0, 25)
StatusLabel.Position = UDim2.new(0, 10, 1, -40)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "Status: Waiting for key..."
StatusLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
StatusLabel.Font = Enum.Font.GothamBold
StatusLabel.TextSize = 15
StatusLabel.TextXAlignment = Enum.TextXAlignment.Center
StatusLabel.Parent = KeyFrame
StatusLabel.ZIndex = 3

-- Static Text Box-like Container (Under Toggle Buttons)
local StaticTextBox = Instance.new("Frame")
StaticTextBox.Size = UDim2.new(0, 460, 0, 100)
StaticTextBox.Position = UDim2.new(0.5, -230, 0, 290)
StaticTextBox.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
StaticTextBox.BorderSizePixel = 2
StaticTextBox.BorderColor3 = Color3.fromRGB(80, 80, 100)
StaticTextBox.Parent = KeyFrame
StaticTextBox.ZIndex = 3

local CornerStatic = Instance.new("UICorner")
CornerStatic.CornerRadius = UDim.new(0, 10)
CornerStatic.Parent = StaticTextBox

-- "READ ME!" Title
local ReadMeTitle = Instance.new("TextLabel")
ReadMeTitle.Size = UDim2.new(1, -20, 0, 30)
ReadMeTitle.Position = UDim2.new(0, 10, 0, 8)
ReadMeTitle.BackgroundTransparency = 1
ReadMeTitle.Text = "READ ME!"
ReadMeTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
ReadMeTitle.Font = Enum.Font.GothamBold
ReadMeTitle.TextSize = 20
ReadMeTitle.TextXAlignment = Enum.TextXAlignment.Center
ReadMeTitle.TextYAlignment = Enum.TextYAlignment.Top
ReadMeTitle.Parent = StaticTextBox
ReadMeTitle.ZIndex = 3

-- Static Text
local StaticText = Instance.new("TextLabel")
StaticText.Size = UDim2.new(1, -20, 1, -35)
StaticText.Position = UDim2.new(0, 10, 0, 30)
StaticText.BackgroundTransparency = 1
StaticText.Text = "The key is very simple and quick to retrieve, all you have to do is join the discord."
StaticText.TextColor3 = Color3.fromRGB(200, 200, 200)
StaticText.Font = Enum.Font.Gotham
StaticText.TextSize = 16
StaticText.TextWrapped = true
StaticText.TextXAlignment = Enum.TextXAlignment.Center
StaticText.TextYAlignment = Enum.TextYAlignment.Center
StaticText.LineHeight = 1.6
StaticText.Parent = StaticTextBox
StaticText.ZIndex = 3

-- Add "X" button to close the GUI
local CloseButton = Instance.new("TextButton")
CloseButton.Size = UDim2.new(0, 30, 0, 30)
CloseButton.Position = UDim2.new(1, -40, 0, 10)
CloseButton.Text = "X"
CloseButton.Font = Enum.Font.GothamBold
CloseButton.TextSize = 20
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
CloseButton.Parent = KeyFrame
CloseButton.ZIndex = 4

CloseButton.MouseButton1Click:Connect(function()
    -- Unload with a smooth animation before destroying
    KeyFrame:TweenSize(UDim2.new(0, 500, 0, 450), Enum.EasingDirection.In, Enum.EasingStyle.Sine, 0.5, true)
    wait(0.5)
    KeyGui:Destroy()
end)

-- Button Hover Effects with animation
SubmitButton.MouseEnter:Connect(function()
    SubmitButton:TweenSize(UDim2.new(0, 170, 0, 50), Enum.EasingDirection.Out, Enum.EasingStyle.Bounce, 0.2, true)
end)

SubmitButton.MouseLeave:Connect(function()
    SubmitButton:TweenSize(UDim2.new(0, 160, 0, 45), Enum.EasingDirection.Out, Enum.EasingStyle.Bounce, 0.2, true)
end)

GetKeyButton.MouseEnter:Connect(function()
    GetKeyButton:TweenSize(UDim2.new(0, 180, 0, 50), Enum.EasingDirection.Out, Enum.EasingStyle.Bounce, 0.2, true)
end)

GetKeyButton.MouseLeave:Connect(function()
    GetKeyButton:TweenSize(UDim2.new(0, 160, 0, 45), Enum.EasingDirection.Out, Enum.EasingStyle.Bounce, 0.2, true)
end)

-- Add effects when the GUI is loaded/unloaded
KeyFrame:TweenSize(UDim2.new(0, 500, 0, 450), Enum.EasingDirection.Out, Enum.EasingStyle.Bounce, 0.5, true)  -- Smooth animation for loading
wait(1)
KeyFrame:TweenSize(UDim2.new(0, 500, 0, 450), Enum.EasingDirection.Out, Enum.EasingStyle.Sine, 0.5, true)
-- Existing SubmitButton functionality
GetKeyButton.MouseButton1Click:Connect(function()
    if setclipboard then
        local success, err = pcall(function()
            setclipboard("https://discord.gg/EZvYf4ntmK")
        end)

        if success then
            Luna:Notification({
                Title = "Link Copied!",
                Content = "The Discord invite link has been copied to your clipboard.",
                Icon = "link",
                ImageSource = "Material",
                Duration = 5
            })
        else
            Luna:Notification({
                Title = "Copy Failed",
                Content = "Failed to copy the invite link. Please try again.",
                Icon = "error",
                ImageSource = "Material",
                Duration = 5
            })
            warn("Clipboard error: " .. tostring(err))
        end
    else
        Luna:Notification({
            Title = "Clipboard Unsupported",
            Content = "Your exploit does not support clipboard functionality.",
            Icon = "error",
            ImageSource = "Material",
            Duration = 5
        })
        warn("Clipboard function 'setclipboard' not available.")
    end
end)

-- Define your correct key
local CorrectKey = "777"

SubmitButton.MouseButton1Click:Connect(function()
    local enteredKey = KeyBox.Text

    if enteredKey == CorrectKey then
        StatusLabel.Text = "Status: Key Verified!"
        StatusLabel.TextColor3 = Color3.fromRGB(100, 255, 100)

        Luna:Notification({
            Title = "Access Granted!",
            Content = "You have successfully unlocked the hub.",
            Icon = "check_circle",
            ImageSource = "Material",
            Duration = 4
        })

        wait(1)

        -- Optional: animate GUI closing
        KeyFrame:TweenSize(UDim2.new(0, 500, 0, 0), Enum.EasingDirection.In, Enum.EasingStyle.Sine, 0.4, true)
        wait(0.5)
        KeyGui:Destroy()

        -- Load your actual hub script
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Crypoth/SCRIPT-SCRIPT/refs/heads/main/here"))()
    else
        StatusLabel.Text = "Status: Invalid Key. Please try again."
        StatusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)

        Luna:Notification({
            Title = "Invalid Key",
            Content = "The key you entered is incorrect.",
            Icon = "error",
            ImageSource = "Material",
            Duration = 5
        })
    end
end)
